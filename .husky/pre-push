
echo "Running TypeScript checks..."
pnpm type-check

if [ $? -ne 0 ]; then
  echo "TypeScript checks failed. Aborting push."
  exit 1
fi

echo "Running e2e tests..."
pnpm test:e2e

if [ $? -ne 0 ]; then
  echo "E2E tests failed. Aborting push."
  exit 1
fi
